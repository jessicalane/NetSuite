//This is a script I am using to practice creating consolidated PDFs with the pdfset BFO functionality.

/**
 *@NApiVersion 2.x
 *@NScriptType ScheduledScript
 */
define([], function() {

    function execute(context) {
        
    }

    return {
        execute: execute
    }
});
